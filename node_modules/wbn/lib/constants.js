export const B1 = 'b1';
export const B2 = 'b2';
export const DEFAULT_VERSION = B2;
export const APPROVED_VERSIONS = [B1, B2];
export function isApprovedVersion(param) {
    return APPROVED_VERSIONS.includes(param);
}
