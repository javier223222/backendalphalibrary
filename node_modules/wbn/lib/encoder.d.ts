import { FormatVersion } from './constants.js';
export interface Headers {
    [key: string]: string;
}
declare type OverrideHeadersFunction = (filepath: string) => Headers;
declare type OverrideHeadersOption = Headers | OverrideHeadersFunction | undefined;
export declare class BundleBuilder {
    private sectionLengths;
    private sections;
    private responses;
    private currentResponsesOffset;
    private compatAdapter;
    constructor(formatVersion?: FormatVersion);
    createBundle(): Uint8Array;
    addExchange(url: string, status: number, headers: Headers, payload: Uint8Array | string): BundleBuilder;
    setPrimaryURL(url: string): BundleBuilder;
    setManifestURL(url: string): BundleBuilder;
    private addSection;
    private addResponse;
    private addIndexEntry;
    private fixupIndex;
    private createTopLevel;
    get formatVersion(): FormatVersion;
    private createCompatAdapter;
}
export declare function isHeaders(obj: any): obj is Headers;
export declare function combineHeadersForUrl(headers: Headers, overrideHeadersOption: OverrideHeadersOption, url: string): Headers;
export {};
